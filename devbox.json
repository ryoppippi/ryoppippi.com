{
	"$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.10.6/.schema/devbox.schema.json",
	"packages": ["bun@latest"],
	"shell": {
		"init_hook": ["bun i --frozen-lockfile"],
		"scripts": {
			"build": "bun run build",
			"dev": "bun run dev --open"
		}
	}
}
