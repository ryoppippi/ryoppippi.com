pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*"
      run: pnpm eslint --cache --fix {staged_files}
    sort-package-json:
      glob: "package.json"
      run: pnpm sort-package-json {staged_files}